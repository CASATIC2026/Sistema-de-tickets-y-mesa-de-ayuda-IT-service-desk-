server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index solicitante/Login.html index.html;

    location / {
        try_files $uri $uri/ /solicitante/Login.html;
    }
}
